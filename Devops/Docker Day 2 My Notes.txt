========================================================
Docker Day 2
================================================
Im Pulling an image from repository
#docker pull httpd
that image will be converted to container
#docker images
#docker run -itd --name cont1 -p "8081:80" httpd
#docker ps

#docker exec -it container id /bin/bash
inside the container going to add Vi editor
#apt-get update -y
#apt-get install vim
#cd htdocs/
#vi index.html
edit and save

Now copy publi ip:8081-->Shows the page

Unfortunately if the container is deleted
=================================
Lets see if i can get the backup
Now i Going to Convert the Container to Image
#Docker ps
#docker commit container id myimage
#docker images
Shows 2 images
now even if the containers is deleted then i can pull the image

if the image is deleted
the image can be pushed to hub
or
The image can be taken as cloud backup or device local backup

root#docker save -o /root/ruby(any name).tar myimage
it will be stored as .tar

now i have taken local backup

to delete the image
#docker rmi myimage
#ls
#docker load -i ruby.tar
that file will convert to image

then use the container commmands to run the backup of image
=========================================
now to take the image as backup from dockerhub

#docker tag myimage mathew666/ro

mathew666 is username of docker

#docker images
shows the username

Now to push
#docker login
username:
password:

#docker push mathew666/ro
if the hub name is not give only myimage then it wont get pushed

if you login intoo docker u can see the images

================================================================================================
Now we are going to write image file and run as container

Vim
Wget
CentOs

#vi Dockerfile
D should be caps

FROM centos:7
MAINTAINER arun
RUN yum install vim wget -y

Now a Dockerfile is written,Now to convert to image
#docker build -t anyname .
the . represents the dockerfile location

#docker images 
shows the winimage file

#
#docker ps
#vi
#wget paste url
#ls
Index.html
cat index.html

Now we have created a container

==============================================================
now i have installed vim,wget,centos
now add git
exit
#vi Dockerfile

FROM winimg
MAINTAINER arun
RUN yum install git -y

in winimg it already has centos,vim,wget
so just adding git
now again 
#docker build -t mathew:v1 .
#docker images
shows 2 images

#docker run -itd --name cont2 winimg:v1
#docke exec -it container id /bin/bash
check for vim,wget,git
git --version
=======================================================================
date

alias arun=date
by running this command if i type aru it shows date

vi .bashrc
shows the alias commands i have edited

============================================

Docker engine is deployment
docker compose
for each container we write a file


=========================================================================================
   
 
   55  docker tag jenkins:works mathew666/Jenkins:Dockerfile
   56  docker tag jenkins:works mathew666/jenkins:dockerfile
   57  docker tag jenkins mathew666/jenkins:dockerfile
 
   59  docker push mathew666/jenkins:dockerfile

   61  docker tag prometheus mathew666/prometheus:prometheus dockerfile
   62  docker tag prometheus mathew666/prometheus:dockerfile

   64  docker prometheus mathew666/prometheus:dockerfile
   65  docker push mathew666/prometheus:dockerfile

   67  docker tag terraform mathew666/terraform:dockerfile
   68  docker push mathew666/terraform:dockerfile
   69  docker tag terraform mathew666/jenkins:dockerfile
   70  docker push mathew666/jenkins:dockerfile
  



















