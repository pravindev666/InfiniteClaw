Jenkins Admin-Master & Slave Configuration

Descrption:
There Will be many environments like Developers,Production,testing environments
Example if there is 20 server i have to install jenkins in all servers and build
In One Server Will be master and the remaining Server will copy or shadow whatever done by the master server
The master server will connect with the slave server and jenkins only be run in the master server and not the slave
For Example A,B,C will be slave to one console that will be the master which will run and control A.B.C
 
For practical lets take one master and two slave

-->Now to give admin access to master,Don't install jenkins
go to master and give #ls -lrta
there will be .ssh folder
#cd .ssh
will have authorized keys
#cat authorized_keys
shows the key
-->In real time keys wont be given
#ssh localhost---->to check if this machine can connect with another machine
if you give yes it will say permission denied
now we venture into RSA alogritham concept
#touch id_rsa------>now we are creating a file named id_rsa which will give this server privilages
#touch id_rsa.pub
#ls -lrta---->Lists the 2 Files id_rsa and id_rsa.pub
(id_rsa is a compulsary file name)
#cp authorized_keys id_rsa.pub----->will copy the key to id_rsa.pub
#ls -lrta------> to check if the copy happended u can see the 0 change to some number which means the copy happened)  
NOW WE HAVE FINISHED THE PUBLIC  KEY BUT THE PRIVATE KEY is in our laptop
WE HAVE TO BRING THE PRIVATE key to our server

#vi id_rsa
Go to puttygen load the ppk file keypair go to conversions Export openssh key(Force)
save the vi
#ls -lrta
#chmod 400 *
will change the read write execute permission
#ls -lrta to check the read write permissions
#ssh localhost
problems faced during was check if the ppk file is pasted correctly in id_rsa vi file by #cat id_rsa
also come out of root
ubuntu@ip-172-31-32-178:~$----->Should be like this and not like
root@ip-172-31-32-178:~$.....Not like this
to jump from root to ubuntu---->#su -c "bash" ubuntu

now it will have privilage it will welcome you
Now Install jenkins and java

In slave create /home/ubuntu/jenkins
#mkdir jenkins in the slaves

Now Connecting the master and slave using agenting method

go to master
connect to jenkins
enter ip address:8080
-->For Password
#cat /var/lib/jenkins/secrets/initialAdminPassword
copy and paste the password

In master go to manage Jenkins--->Manage Nodes
it will show 1 node
click on the new node and keep a name as agent1(the only it will come)--->Permananent Agent
-->Create
Now give the slave details
executors-->2...at a time how may job it can run at a time in real time usually 50 or 60 is done
based on server speed,cpu,memory,space we take 2
remote root directory for slave is /home/ubuntu/jenkins
labels-->any job name ex.zerodha...in real time 10 server will run for zerodha another 10 will run for hdfc
but 1 master machine will control them
from master we can click on the label it will represent that server
its a cluster concept

lauch method-->lauch agents via ssh
slave ip addresss
credentials
add-->jenkins-->SSH with private key---->username,Id....ubuntu-->Private key-->enter directly paste it-->
the in Host Key Verification Strategy give--->Manual trusted key verification startergy
then save--->in nodes it will show--> if refresh will become in sync-->master and slave will connect

go to slave machine
#ll
show remoting means that master and  slave is conected

if ssh localhost is connected no need to to do touch etc
 

now new item-->free style project--->build steps add command then go to general in left corner
restrict where to run--->label expression--->type the name agent1 or label name--->appy and save
back to dashboard------>Run the Job and refresh
go to master and 
# cd /var/lib/jenkins/workspace/
now i have run a job it should be reflected in the master but it wont but it will in the slave
in slave machine go to jenkins--->workspace-->the job name will appear

SECURITY ROLE

in the master machine
there are two jobs...Developers and tester

if developer comes he should work in developer job and tester comes and works in the tester job
here come role based stratergy

go to manage jenkins---> security--->role based wont be there so go to manage plugin in the manage jenkins
go to manage jenkins---.system configuration--->plugins-->manage plugin-->in search type role--->
 and role based authorization shows--->install in top right corner

again go to manage jenkins---> security--->role based stratergy in authorization---> apply and save
again go to manage jenkins--->user--->create user
again go to manage jenkins----->manage roles--->add role staff and give read only ticks
for item role
ex developer
pattern->dev.* he can do his job but only  his job or domain nor on the testers job
pattern->test.*
apply and save

now i have say to jenkins which user is the developer and who is the tester

go to assign roles--->type the name of the user given-->tick on the staffs
go to item role---->type user name and tick the developer and the tester for the user

logout and login using the user credentials
now as user i cannot access settings

