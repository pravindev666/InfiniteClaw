JENKINS
Description:
-->Introduction to Jenkins
-->Creation of a Ubuntu server
-->Installing Jenkins Keys,Java,Jenkins in the Ubuntu Server
-->Login to Jenkins Using 8080 port number,Installing plugins
-->Creating a Job,Running Shell Commands,Upstream
-->Sequential Run
-->Pipeline



Create Ubuntu Server
login terminal:ubuntu

#sudo su -

-->Download the ubuntu packages /Jenkins Debian Packages
# sudo wget -O /usr/share/keyrings/jenkins-keyring.asc \
    https://pkg.jenkins.io/debian-stable/jenkins.io-2023.key

# echo deb [signed-by=/usr/share/keyrings/jenkins-keyring.asc] \
    https://pkg.jenkins.io/debian-stable binary/ | sudo tee \
    /etc/apt/sources.list.d/jenkins.list > /dev/null

# sudo apt-get update
# sudo apt-get install fontconfig openjdk-17-jre
# sudo apt-get install jenkins

enter ip address:8080
-->For Password
#cat /var/lib/jenkins/secrets/initialAdminPassword
f3dd91e131934fa3a30ac2e0c6a1f0cb
-->Paste this code in Jenkins
-->Install plugins
-->Create new Username and password
-->Now welcome page Shows
-->New Item
-->Name
-->Free style project
--->Configuration Page Shows
--->left hand side build steps -->add build steps-->execute shell-->pwd-->apply and save
-->back to Dashboard
Weather report
run the play button it shows 100 percent
for sabotaging,now go the job by clicking the name 
-->Configure
-->change pwd to data
-->then go to dashboard and run and refresh it
-->now error shows
-->code deploy is bad,which means the job is bad
again go to configure change data to date and save and apply
now run the job

now click the job name--->build name scroll down to see the error reports
now click the error reports --->console output-->shows reason for failure
copy ad paste the reason for failure to search for answers

TO Delete a JOB

click job and rename or delete on the left side

Sequential Run

Dev-Devloper for Name-->free style project
same as before shell-->pwd

test---->free style
same as before

prod-->for production-->freestyle
same as before

LANDING PATH

#cd /var/lib/jenkins-------------->Default landing Path
#ls------------------------------------>to view the folders and sub folders
#cd Workspace ----->is the landing path
#ls----------------->shows the Jobs with Name
after running one by one the all three jobs will update in the workspace

go to dev and configure
post build action----->build another project---->test---->Trigger only if build is stable

go to test and configure
post build action----->build another project---->prod---->Trigger only if build is stable

go to dashboard click on dev or test it shows upstream project or downstream project

RUN the Dev job

so now if the job should automatically run even though i should not click run

go to dev--->configure-->build trigger--->build periodically----->cron expression * * * * * means every minute
and save changes

now it runs automatically every minute

Trigger build remotely
go to dev--->configure-->build trigger--->Trigger builds remotely----->authetication token give any number
JENKINS_URL/job/dev/build?token=TOKEN_NAME---->Copy this
JENKINS_URL/job/dev/build?token=6666
http://18.140.68.166:8080/job/dev/build?token=6666

so the purpose of the link is if i click this link the job will run

now create a different job
UNDER PIPELINE WRITE GROOVY SCRIPT
SAMPLE CODE WITHOUT ANY COMMAND WE USED SIMPLE SCRIPT TO TEST A PIPELINE: 
APPLY AND SAVE
pipe----->pipeline project
after pasting the below code----->save and apply
node
{
    stage('Commit')
    {
        echo "This is Code Download from GIT Project Repository.................."
    }
    stage('Build')
    {
        echo "This is Build project using maven.................."
    }
    stage('Test')
    {
        echo "This is Test project Implementation using Selenium................."
    }
    stage('Release')
    {
        echo "This is Delivery using Docker......................................"
    }
    stage('Monitor')
    {
        echo "This is Application Logs Monitorinig using tool Splunk............."
    }
}


go to build now to see the running
see console output

now go to pipe--->configure--->Pipeline---->pipeline script from scm--->scm--->git
the jenkins file should be copied and paste in git file and the file name to be entered in the jenkinsfile
even case sensitive and after commiting run









FOR LINUX

sudo yum update –y
sudo wget -O /etc/yum.repos.d/jenkins.repo \
    https://pkg.jenkins.io/redhat-stable/jenkins.repo
sudo rpm --import https://pkg.jenkins.io/redhat-stable/jenkins.io-2023.key
sudo yum upgrade
sudo yum install jenkins -y
sudo systemctl enable jenkins
sudo systemctl start jenkins
sudo systemctl status jenkins
