Ansible 

PLAYBOOK

Commands:
1.yaml/yml - Extensions
2.INDENTATION -Spacing (Visual Studio Code) -Save.yml
3.Notepad++ ---Server (Directly in VI Editor)


----Apache Install-Index.html-app hosting
----Loop concept -Multiple Package
----Notify
----Handlers

#vi index.html
Type any thing in the Index.html
(Ex.Welocme to this World)

go to module
copy to remote locations
https://docs.ansible.com/ansible/2.9/modules/copy_module.html#copy-module
copy the example
copy and paste the file in 

Write Yaml file that its Installs httpd and starts the httpd service
and the yaml file should specifies what to display and directing the html file path and file name
mode:0777 means The owner can read, write, and execute the file.,The group can read, write, and execute the file.
All other users can read, write, and execute the file.

0644: Owner can read and write, group and others can only read.
0755: Owner can read, write, and execute, group and others can read and execute.


#vi apache.yml

ansible-playbook -i slave.txt apache.yml --syntax-check
ansible-playbook -i slave.txt apache.yml


LOOP ANSIBLE-iteration-Sequence of doing tasks
Means the we can install many services such as httpd,php,java etc in a single playbook command in all slaves
https://docs.ansible.com/ansible/latest/playbook_guide/playbooks_loops.html#standard-loops
Multiple packages can be installed


vi loops.yaml
ansible-playbook -i slave.txt loops.yaml --syntax-check
ansible-playbook -i slave.txt loops.yaml


*if the packages are already installed-green
*if the packages are new-yellow
*if the packages name is wrong-red


Jenkins-Playbook:
1.Download Jenkins Repository
go to ansible modules-yum repository
copy the below first examples
for base url:https://www.jenkins.io/doc/book/installing/
click linux--->redhat (https://pkg.jenkins.io/redhat-stable/jenkins.repo)
paste the link in ggole it will show the base url copy that base url
baseurl=http://pkg.jenkins.io/redhat-stable

2.GPG  Key-Data is in open source -To encrypt it
go to ansible modules-->yum repository copy the gpgkey name only not the link
gpgkey for jenkins-->
go to the jenkins and the second step will have the li

3.Install Java
4.Install Jenkins
copy the loop module


5.Start Jenkins




#jenkins.yaml
paste the yaml code

ansible-playbook -i jenkins.yaml --syntax-check
ansible-playbook -i slave.txt jenkins.yaml

HANDLERS-TRIGGERS
Port numbers:8080
change to 9000

if these kind of change is done then we have to tell the machine to restart

notify:restart jenkins
then handlers wil restart

in case if the interviewrs asks if how copy module is used
say from source will be in the control node(Master) will push it to the 
managed node(Slave)
 i will push using src and node

Most playbooks will have 10 pages without loop command
if ansible is running dont give ctrl+c it wills top the process


to access the slave from master
#ssh ec2-user@ip address of the slave 
#sudo vi /etc/sysconfig/jenkins

search the port number in jenkins


but the major update is
8080 port number cannot be changed from
august 2022

#vi newjenkins.yaml


ansible-playbook -i newjenkins.yaml --syntax-check
ansible-playbook -i slave.txt newjenkins.yaml

ssh ec2-user@ip address of the slave 
#sudo vi /etc/sysconfig/jenkins
to check the changed port number
ansible-playbook -i newjenkins.yaml --syntax-check

but jenkins wont start if the port number is changed