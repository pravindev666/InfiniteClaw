00:41:39	Manoj:	aws s3 ls (list the bucket) Create bucketaws s3 mb s3://<buket name> --region <region>Example:aws s3 mb s3://18sep2025 --region us-east-1Transfer the file local pc to ec2-user by using winscp toolCopy the file local server to s3 bucketaws s3 cp <filename> <bucket name>Example:aws s3 cp banner.png s3://18sep2025
00:48:05	Manoj:	•	For Amazon Linux 2 or the Amazon Linux AMI, the user name is ec2-user.•	For a CentOS AMI, the user name is centos or ec2-user.•	For a Debian AMI, the user name is admin.•	For a Fedora AMI, the user name is fedora or ec2-user.•	For a RHEL AMI, the user name is ec2-user or root.•	For a SUSE AMI, the user name is ec2-user or root.•	For an Ubuntu AMI, the user name is ubuntu.•	For an Oracle AMI, the user name is ec2-user.•	For a Bitnami AMI, the user name is bitnami.
01:30:23	Sassie Dharan:	Clear..
