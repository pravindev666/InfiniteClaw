ANSIBLE

*Its a DevOps Tool
*Functionality-Configuration management/Automation
*Apache Installation is a Configuration
(Complete hosting) 
*Agentless-Master-Slave Concept
*Push Mechanism
*Control Node--->Master
*Managed Node--->Slave
*Requires Python-Master(Ansible) & Slave

DevOps Periodic Table---Google it

Can i Create a VPC using Ansible?
Yes,We Can But Not Effective as Terraform

Terraform Cannot do Configurations,Cannot change port numbers
or install.

From Only Control Node all managed nodes will be accesed and managed node
will not be touched

Now Task
Control Node-1
managed node-2

Launch a linux machine  
storage is 30gb
paste the below in metadata

#! /bin/bash
yum install python-pip -y
pip install ansible

pip is python installation package

Ansible has 2 major files
1.Inventory File---slaves.txt
2.configuration file----ansible.cfg

these 2 files will be control node--Master 

TOOLS Needed for Ansible
PuTTY
Pageant
Visual Studio Code

google puTTy.org download
click download putty
pageant.exe (an SSH authentication agent for PuTTY, PSCP, PSFTP, and Plink)
search for pagent and pagent will be running in task bar right side
double click the icon and add the keys
and when accessing puTTy 
AUTH--->Allow Agent forwarding
the use for paegent is to not add keys frequently



In Control Node(MASTER)
#ansible --version
(to check version)
ansible core 2.15.12
 
#vi slave.txt
create this in ec2-user and not in root
#su - ec2-user
Paste the ip address of slaves
even if there are more than 100 then paste each and every ip address

Google "Ansible configuration File"
https://docs.ansible.com/ansible/latest/installation_guide/intro_configuration.html#configuring-ansible
https://gist.github.com/alivx/2a4ca3e577ead4bd38d247c258e6513b
paste the above is configuration file below
#vi ansible.cfg

Adhoc-Unexpected job

adhoc command -sibgle command
apache install
yum install httpd

now if the slave receive the master
ping connectibvity

ansible commands are usually start with ansible

#ansible all -i slave.txt -m ping
-i-->check this inventory
-m-->Modules
run this in master
(Even this not executed in root)

#vi ansible.cfg
esc / for search
/host and n command for next host
then remove the hashtag in host_key
# uncomment this to disable SSH key host checking
host_key_checking = False

#ansible all -i slave.txt -m ping

54.151.248.92 | SUCCESS => {
    "ansible_facts": {
        "discovered_interpreter_python": "/usr/bin/python"
    },
    "changed": false,
    "ping": "pong"
}

This SHOULD BE THE RESULT

Now to Install  apache in slaves
the task that should be done is the module
All Modules---Ansible doc google it
https://docs.ansible.com/ansible/2.9/modules/list_of_all_modules.html

YUM "name=httpd"

STATE:
present/latest ------install
absent -uninstall

start - started
stop -stopped
restart-restarted

go to slave.txt
[india]
ip address
[lanka]
ip address

direct ip address wont work

#ansible india -i slave.txt -m yum -a "name=httpd state=latest" -b
if yellow colour script comes it is a success

#ansible india -i slave.txt -m service -a "name=httpd state=started" -b
if yellow colour comes it is a success

to know if the apache is working go to copy the
ansible slave ip and paste i google

#ansible india -i slave.txt -m service -a "name=httpd state=stopped" -b
to stop apache

ansible india -i slave.txt -m yum -a "name=httpd state=removed" -b
to remove the httpd
